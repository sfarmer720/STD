using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using UnityEngine.Events;

public class UnitCommonFunctions
{

    //Splash: causes damage / healing in radius around a tile
    public void Splash(Tile initTile, int dmgType, int dmg)
    {
        //cycle tile neighbors
        for(int i = 0; i < initTile.neighbors.Count; ++i)
        {

        }
    }
}